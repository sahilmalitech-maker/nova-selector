# GitHub Publishing Notes

## Recommended Setup

Keep the GitHub repository public for source, documentation, and packaging files.

Do not commit the built desktop artifacts directly into git. Publish the installers through GitHub Releases instead.

## Release Assets To Upload

- macOS: `artifacts/macos/NovaImageScout-macOS.dmg`
- Windows portable build: ZIP the entire `artifacts/windows/Nova Image Scout/` folder and upload that ZIP
- Windows installer build: upload the generated installer from `artifacts/windows-installer/`

## Important Windows Packaging Detail

`Nova Image Scout.exe` is not a standalone portable executable. It depends on the `_internal/` folder beside it.

That means the portable Windows release must be distributed as the full folder, usually as a ZIP archive.

For a cleaner end-user install experience, the preferred Windows public download is the Inno Setup installer generated by `packaging/build_windows_installer.bat`. That installer:

- installs into `Program Files`
- creates a Start Menu entry
- offers a desktop shortcut
- registers an uninstaller in Windows

## GitHub Limits Warning

The current Windows build is very large because it bundles OCR runtime files and multiple AI runtimes together.

If the final Windows ZIP exceeds GitHub's release asset limits, the packaging should be slimmed down before publishing. The macOS DMG is already a better fit for GitHub release uploads.

## Suggested Repository Name

`nova-image-scout`

## Suggested Repository Description

Public AI-powered desktop app for image retrieval, OCR-assisted search, visual similarity matching, and photographer-grade photo culling.
